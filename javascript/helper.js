(function() {
    var phone = window.document.getElementById('id_phone');
    var code = window.document.getElementById('id_code');

    if (phone) {
        phone.focus();
    }

    if (code) {
        code.focus();
    }

})();